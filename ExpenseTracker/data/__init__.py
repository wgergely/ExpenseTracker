"""
ExpenseTracker data package: analytics, models, and views.

This package provides:
    - data: High-level API for loading, filtering, and summarizing expense data
      from the local cache (get_data, get_trends) with settings-driven metadata.
    - model: Qt table models (ExpenseModel, TransactionModel) for displaying
      categorized summaries and transaction lists.
    - view: Qt views and delegates for rendering charts, tables, and interactive widgets
      to visualize expense analytics.
"""
