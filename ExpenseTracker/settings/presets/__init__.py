"""Presets subpackage: snapshot and restore of application configurations.

This package provides:
    - lib: core logic for creating, loading, and managing preset archives of ledger and auth files
    - model: Qt models for listing, activating, and editing presets
    - view: Qt views and dock widget for interacting with presets in the UI
"""
